<!doctype html>
<html lang="en">
  <head>
    <%= partial "partials/head" %>
    <%= stylesheet_link_tag "code_highlight" %>
  </head>
  <body class="<%= page_classes %>">
    <%= partial "partials/header" %>
    <section id="site-content-area">
      <header class="site-content-area-header padding-large">
        <div class="mid-width">
          <h1><%= current_page.data.title %></h1>
          <% if current_page.data.has_key?('subtitle') %>
            <h2 class="subtitle"><%= current_page.data.subtitle %></h2>
          <% end %>
        </div>
      </header>

          <article class="blog-post">
            <header>
              <span class="blog-post__author"><%= current_page.data.author %></span>
              <% if defined? current_page.date %>
                ,
                <span class="blog-post__date">
                  <%= current_page.date.strftime('%d %B, %Y') %>
                </span>
              <% end %>
            </header>
            <div class="blog-post__body"><%= yield %></div>
          </article>

    </section>
    <%= partial "partials/footer" %>
  </body>
</html>
