<!doctype html>
<html>
  <head>
    <%= partial "partials/head" %>
    <%= stylesheet_link_tag "code_highlight" %>
  </head>
  <body class="<%= page_classes %>">
    <%= partial "partials/header" %>
    <div id="site-content-area">
      <div class="wrapper">
        <h1><%= current_page.data.title || current_article.title %></h1>
        <div class="content-column">
          <%= current_page.data.author %><% if defined? current_page.date %>,
            <%= current_page.date.strftime('%d %B, %Y') %>
          <% end %>

          <%= yield %>
        </div>
        <div class="sidebar-column">Some Sidebar content here</div>
      </div>
    </div>
  </body>
</html>
