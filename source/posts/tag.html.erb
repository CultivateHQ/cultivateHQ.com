---
title: Blog
subtitle: Thoughts &amp; Tutorials
pageable: true
per_page: 10
---

<div class="layout-two-column max-width">
  <div class="layout-two-column__left">
    <div class="page-content min-width padding-large page-content--blog">
        <% blog.tags.each do |tag, articles| %>
        <%# blog.tags[tag].each do |articles| %> 
          <% articles.each do |article| %>
              <article class="blog-post">
                <h2><%= link_to article.title, article %></h2>
                <div class="blog-post__summary"><%= article.summary %></div>
                <div class="meta">
                  <span class="blog-post__author">By <%= article.data.author %></span>
                  - <span class="blog-post__date"><%= article.date.strftime('%d %B, %Y') %></span>
                </div>
              </article>
          <% end %>
        <% end %>
      </div>
    </div>

<div class="layout-two-column__right padding">
<aside class="widget">
  <%= partial 'partials/blog_pagination',
        locals: {
          prev_page: prev_page,
          next_page: next_page,
          num_pages: num_pages,
          page_number: page_number } %>
</aside>
</div>
</div>
</section>
